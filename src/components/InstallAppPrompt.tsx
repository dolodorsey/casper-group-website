'use client';

import { useEffect, useState } from 'react';

type PromptEvent = Event & { prompt: () => Promise<{ outcome: 'accepted' | 'dismissed' }> };

function forceInstall(){try{return new URLSearchParams(location.search).get('install')==='1'}catch{return false}}
function InstallQr(){
  const [qr,setQr]=useState('');
  useEffect(()=>{if(typeof window==='undefined'||window.innerWidth<760)return;try{const u=new URL(location.href);u.hash='';u.search='';u.searchParams.set('install','1');setQr('https://wfkohcwxxsrhcxhepfql.supabase.co/functions/v1/app-install-qr?url='+encodeURIComponent(u.toString()))}catch{}},[]);
  if(!qr)return null;
  return <aside aria-label="Scan to install app" style={{position:'fixed',right:22,bottom:22,zIndex:2147483002,width:188,padding:12,borderRadius:20,background:'rgba(7,8,11,.97)',border:'1px solid rgba(255,255,255,.16)',boxShadow:'0 24px 70px rgba(0,0,0,.48)',color:'#fff',fontFamily:'Arial,sans-serif'}}>
    <img src={qr} alt="QR code to install this app" width="164" height="164" style={{display:'block',width:'100%',height:'auto',borderRadius:12,background:'#fff',padding:6}}/>
    <strong style={{display:'block',marginTop:10,fontSize:10,letterSpacing:'.14em'}}>SCAN TO GET THE APP</strong>
    <small style={{display:'block',marginTop:5,color:'rgba(255,255,255,.62)',fontSize:9,lineHeight:1.45}}>iPhone: Share → Add to Home Screen → Open as Web App → Add. Android: tap Install App.</small>
  </aside>
}

const CAPTURE_URL='https://wfkohcwxxsrhcxhepfql.supabase.co/functions/v1/marketing-event-capture';
const DISMISS_MS=7*24*60*60*1000;
function get(k:string){try{return localStorage.getItem(k)}catch{return null}}
function set(k:string,v:string){try{localStorage.setItem(k,v)}catch{}}
function visitor(){const existing=get('khg_vid');if(existing)return existing;const id=crypto.randomUUID();set('khg_vid',id);return id}
function isIOS(){return /iphone|ipad|ipod/i.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1)}
function standalone(){return matchMedia('(display-mode: standalone)').matches||Boolean((navigator as Navigator&{standalone?:boolean}).standalone)}
async function track(event_type:string,metadata:Record<string,unknown>={}){try{await fetch(CAPTURE_URL,{method:'POST',headers:{'content-type':'application/json'},keepalive:true,body:JSON.stringify({brand_key:'casper-group',event_type,visitor_key:visitor(),metadata:{event_id:crypto.randomUUID(),app:'casper-group',path:`${location.pathname}${location.search}`.slice(0,500),...metadata}})})}catch{}}

export default function InstallAppPrompt(){
 const[prompt,setPrompt]=useState<PromptEvent|null>(null),[show,setShow]=useState(false),[steps,setSteps]=useState(false),[apple,setApple]=useState(false),[installed,setInstalled]=useState(false);
 useEffect(()=>{if(standalone()){setInstalled(true);return}const a=isIOS();setApple(a);if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>undefined);const dismissed=Number(get('casper:pwa-dismissed')||0),eligible=!dismissed||Date.now()-dismissed>DISMISS_MS;const before=(e:Event)=>{e.preventDefault();setPrompt(e as PromptEvent);if(eligible)setTimeout(()=>setShow(true),1800)};const done=()=>{setInstalled(true);setShow(false);void track('app_install',{platform:a?'ios':'web',variant:'casper_group_pwa'})};addEventListener('beforeinstallprompt',before);addEventListener('appinstalled',done);let timer=0;if(eligible&&a)timer=window.setTimeout(()=>setShow(true),4800);return()=>{removeEventListener('beforeinstallprompt',before);removeEventListener('appinstalled',done);if(timer)clearTimeout(timer)}},[]);
 if(installed||!show)return null;
 const close=()=>{set('casper:pwa-dismissed',String(Date.now()));setShow(false);void track('cta_click',{cta:'pwa_prompt_dismiss'})};
 const install=async()=>{void track('app_install_click',{platform:apple?'ios':'web',variant:prompt?'native_prompt':'instructions'});if(prompt){const result=await prompt.prompt();setPrompt(null);if(result.outcome==='accepted')setShow(false);return}setSteps(true)};
 return <div className="casperInstall" role="dialog" aria-modal="true" aria-label="Install Casper Group">
    <InstallQr/><section><button className="close" onClick={close} aria-label="Close">×</button><div className="scene" aria-hidden="true"><div className="glow"/><div className="phone"><i/><b>C</b><small>CASPER</small></div><div className="ghost">C</div></div>{!steps?<div className="copy"><p className="kicker">THE CASPER UNIVERSE / ON YOUR PHONE</p><h2>KEEP<br/><em>CASPER</em><br/>CLOSE.</h2><p>Concepts. Menus. Catering. Locations. Orders. One Home Screen entrance to the Casper Group universe while every restaurant keeps its own identity.</p><div className="tags"><span>CONCEPTS</span><b>•</b><span>CATERING</span><b>•</b><span>ORDER</span></div><button className="cta" onClick={install}><span>{prompt?'INSTALL CASPER':'ADD CASPER'}</span><strong>↗</strong></button><button className="later" onClick={close}>Keep exploring</button></div>:<div className="copy"><p className="kicker">{apple?'IPHONE / HOME SCREEN':'INSTALL / HOME SCREEN'}</p><h2>THREE TAPS.<br/><em>WHOLE UNIVERSE.</em></h2><ol><li><b>01</b><div><strong>{apple?'Tap Share':'Open browser menu'}</strong><small>{apple?'Use Safari’s Share button.':'Open your browser install menu.'}</small></div></li><li><b>02</b><div><strong>Add to Home Screen</strong><small>Select Add to Home Screen / Install App.</small></div></li><li><b>03</b><div><strong>Tap Add</strong><small>Casper Group lands beside your other apps.</small></div></li></ol><button className="cta" onClick={close}>GOT IT</button></div>}<style jsx>{`.casperInstall{position:fixed;inset:0;z-index:2147483000;display:grid;place-items:end center;padding:16px;background:linear-gradient(180deg,#0001,#000d);backdrop-filter:blur(11px);animation:enter .3s ease both}.casperInstall section{position:relative;width:min(720px,100%);min-height:470px;overflow:hidden;border-radius:30px;padding:32px 24px 23px;background:radial-gradient(circle at 83% 9%,#ffffff20,transparent 30%),linear-gradient(145deg,#161616,#050505 72%);border:1px solid #ffffff2e;box-shadow:0 35px 110px #000d;color:#fff;isolation:isolate}.casperInstall section:after{content:'';position:absolute;inset:0;z-index:-1;opacity:.09;background:repeating-linear-gradient(115deg,transparent 0 15px,#fff2 16px,transparent 17px)}.close{position:absolute;right:14px;top:14px;z-index:5;width:39px;height:39px;border-radius:50%;border:1px solid #fff3;background:#fff1;color:#fff;font-size:25px}.scene{position:absolute;right:-30px;top:8px;width:270px;height:290px;pointer-events:none}.glow{position:absolute;right:18px;top:5px;width:220px;height:220px;border:1px solid #fff5;border-radius:50%;box-shadow:0 0 60px #fff2;animation:pulse 2.4s ease-out infinite}.phone{position:absolute;right:67px;top:42px;width:106px;height:214px;border:4px solid #f5f5f5;border-radius:29px;background:linear-gradient(160deg,#222,#050505);transform:rotate(8deg);display:flex;flex-direction:column;align-items:center;justify-content:center;box-shadow:0 28px 65px #000c}.phone i{position:absolute;top:7px;width:43px;height:10px;border-radius:12px;background:#000}.phone b{width:60px;height:60px;border-radius:18px;background:#f5f5f5;color:#050505;display:grid;place-items:center;font:900 36px/1 Arial;box-shadow:0 0 0 3px #fff2,0 12px 30px #fff2}.phone small{margin-top:13px;color:#ddd;font:900 7px/1 Arial;letter-spacing:.22em}.ghost{position:absolute;right:14px;top:176px;font:900 76px/1 Arial;color:#fff0;-webkit-text-stroke:1px #fff2}.copy{position:relative;z-index:2;max-width:485px;padding-right:90px}.kicker{margin:0 0 11px!important;color:#d8d8d8!important;font:900 10px/1 Arial!important;letter-spacing:.18em!important}.copy h2{margin:0;font:900 clamp(35px,9vw,58px)/.82 Arial,sans-serif;letter-spacing:-.065em}.copy h2 em{font-style:normal;color:#fff;text-shadow:0 0 30px #fff4}.copy>p:not(.kicker){max-width:440px;margin:18px 0 15px;color:#c7c7c7;font:500 14px/1.55 Arial}.tags{display:flex;gap:9px;align-items:center;flex-wrap:wrap;margin-bottom:18px;color:#aaa;font:900 9px/1 Arial;letter-spacing:.11em}.tags b{color:#fff}.cta{width:100%;min-height:55px;border:0;border-radius:14px;background:#f5f5f5;color:#050505;display:flex;justify-content:space-between;align-items:center;padding:0 18px;font:900 13px/1 Arial;letter-spacing:.07em;box-shadow:0 15px 38px #fff2}.cta strong{font-size:22px}.later{width:100%;border:0;background:transparent;color:#888;padding:13px 0 0;font:700 11px/1 Arial}.copy ol{list-style:none;margin:20px 0;padding:0;display:grid;gap:9px}.copy li{display:flex;align-items:center;gap:12px;padding:12px;border:1px solid #fff2;border-radius:13px;background:#fff1}.copy li>b{color:#fff;font:900 11px/1 Arial}.copy li strong,.copy li small{display:block}.copy li strong{font:800 13px/1.2 Arial}.copy li small{margin-top:3px;color:#999;font:500 11px/1.35 Arial}@keyframes enter{from{opacity:0;transform:translateY(13px)}to{opacity:1;transform:none}}@keyframes pulse{0%{transform:scale(.7);opacity:.6}100%{transform:scale(1.18);opacity:0}}@media(min-width:720px){.casperInstall{place-items:center}.casperInstall section{padding:44px 38px 34px}.copy{padding-right:150px}.scene{right:5px;top:36px}}@media(max-width:430px){.casperInstall section{padding:28px 18px 20px;border-radius:25px}.copy{padding-right:40px}.copy h2{font-size:36px}.scene{right:-76px;opacity:.63}}@media(prefers-reduced-motion:reduce){.casperInstall,.glow{animation:none}}`}</style></section></div>
}
